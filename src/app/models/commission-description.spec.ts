import { CommissionDescription } from './commission-description';

describe('CommissionDescription', () => {
  it('should create an instance', () => {
    expect(new CommissionDescription()).toBeTruthy();
  });
});
